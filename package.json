{
  "name": "agents-mcps-starter",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*",
    "tooling/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.1.7",
    "tsx": "^4.16.0"
  },
  "scripts": {
    "prepare": "husky",
    "q:tests": "pnpm dlx tsx tooling/quality-mcp/cli/qcli.ts run --tests"
  }
}
