# OpenClaw Extensions — Agent Instructions (.agent.md)

## 0) Priority (non-negotiable)
1. Follow this `.agent.md`.
2. Follow the current task file in `docs/tasks/`.
3. Follow `docs/roadmap.md` for task status and `docs/backlog/` for epic details.
4. If anything conflicts, prefer: correctness > security > simplicity > consistency.

## 0.0) Plan-first execution (non-negotiable)

**Always present a plan and get user approval BEFORE implementing.** Use the plan tool (EnterPlanMode) to agree on implementation details with the user before writing any code or making changes.

Rules:
- Before any implementation work, enter plan mode and outline: what you will change, which files, and what approach you will take.
- Wait for the user to approve, adjust, or reject the plan before proceeding.
- If a tool call fails or returns no data: retry once, then ask the user how to proceed.
- If a shell command fails: diagnose the error and ask the user before choosing a workaround.

**When to use the questionnaire tool (ask the user):**
Use the built-in questionnaire tool (multiple choice + free text) for **any doubt or uncertainty**, no matter how small. Always include a free-text field so the user can provide context in their own words.
- Ask BEFORE starting implementation, not mid-task.
- Ask whenever you are uncertain about scope, approach, naming, trade-offs, or priorities.
- Ask even if you have a reasonable default — the user may prefer something different.
- Do not ask about things already explicitly specified in `.agent.md`, the task file, or coding standards — but if you have even minimal doubt about interpretation, ask.

**Do not read external repositories or URLs during task execution.**
Read only local files in this repository and installed `node_modules`. If you need to understand a dependency, read its local types or docs — never `git clone`, `curl`, or `gh api` against external repos.

## 0.1) Task Workflow
When the user says **"next task"**:
1. Read `docs/roadmap.md` (`Task Specs`) for current task statuses.
2. Verify dependency epics are `DONE` in `docs/backlog/`.
3. Select the first PENDING task whose dependencies are all met.
4. Ensure task file exists: `docs/tasks/NNNN-<title>.md` (create if missing).
5. Ensure walkthrough exists: `docs/walkthroughs/NNNN-<title>.md` (create if missing).
6. **Plan & Confirm**: Enter plan mode. Present the implementation plan (files to change, approach, key decisions) and wait for user approval.
7. Implement the solution.
8. Run quality gates (build, tests).
9. Update walkthrough with results.
10. Commit with conventional format: `feat|fix|test(scope): message`
11. Mark task DONE in `docs/roadmap.md` and in the task file DoD/status.

See [.agent/rules/autonomous-workflow.md](.agent/rules/autonomous-workflow.md) for full details.

## 1) Language
- **English only** in repository content: code, comments, docs, commit messages.

## 2) Repository documentation rules
- Tasks live in: `docs/tasks/NNNN-*.md`
- Walkthroughs live in: `docs/walkthroughs/NNNN-*.md`
- Every task MUST have a walkthrough with the **same file name**.
- If any work is done, the walkthrough must be updated in the same change set/commit.

Naming conventions:
- Standard: `NNNN-...`
- Fast track: `ft-NNNN-...`
- Code review: `cr-NNNN-...`

## 3) Delivery workflow (how work is performed)
For each task:
1. Read the task file and restate the goal in your own words (briefly, in the walkthrough).
2. Implement the smallest correct solution that satisfies acceptance criteria.
3. Keep changes cohesive. Avoid "drive-by refactors".
4. Update the walkthrough with: decisions, trade-offs, commands run, files changed, tests executed, follow-ups.
5. Ensure quality gates pass locally (or document why not).

### 3.1) Task integrity & scope discipline

#### Task Document Preservation
The task document is the immutable specification. Only the Status field and Definition of Done checkboxes may be modified when completing a task. Implementation notes go in the walkthrough, not the task file.

#### Scope Discipline
Only create, modify, or delete files directly required by the task scope. Note out-of-scope ideas in the walkthrough Follow-ups section.

#### Manual Verification
If a task's Testing Plan includes manual verification steps, complete all automated work first, then explicitly ask the user to perform the manual steps before the task is marked DONE.

## 4) Tech stack
- **Runtime**: Node.js 22+
- **Package manager**: pnpm (workspaces)
- **Language**: TypeScript (strict mode, ESM, `.js` extensions in imports)
- **Testing**: Vitest
- **Schemas**: JSON Schema 2020-12
- **Build**: tsc, tsx for CLI execution

## 5) Monorepo structure
```
extensions/          # OpenClaw extensions (plugins)
  product-team/      # Task engine, workflow, quality, VCS tools
  quality-gate/      # Standalone quality gate CLI (pnpm q:gate)
  model-router/      # Per-agent model routing hook
  telegram-notifier/ # Telegram notification integration
  stitch-bridge/     # Google Stitch MCP design bridge
  virtual-office/    # Virtual office visualization (agent activity, SSE)
packages/
  quality-contracts/ # Shared parsers, gate policy, complexity analysis, and validation contracts
skills/
  adr/               # ADR management skill
  architecture-design/ # Architecture design skill
  backend-dev/       # Backend development skill
  code-review/       # Code review skill
  devops/            # DevOps and infrastructure skill
  frontend-dev/      # Frontend development skill
  github-automation/ # GitHub automation skill
  patterns/          # Architecture patterns skill
  product-owner/     # Product owner skill
  qa-testing/        # QA testing skill
  requirements-grooming/ # Requirements grooming skill
  tdd-implementation/ # TDD implementation skill
  tech-lead/         # Tech lead skill
  ui-designer/       # UI design skill
docs/
  adr/               # Architecture Decision Records
  audits/            # Security/architecture audits
  backlog/           # Epic/backlog specifications (EP01-EP20, AR01)
  tasks/             # Task specifications (0001-0134, cr-*, ft-*)
  walkthroughs/      # Implementation journals
  roadmap.md         # Project roadmap with status tracking
  transition-guard-evidence.md  # What metadata each guard requires
  error-recovery.md  # Error handling patterns for agents
  extension-integration.md     # How extensions interact
```

## 6) Extension contract
Extensions live in `extensions/<name>/` and follow the OpenClaw plugin pattern:
- `package.json` with `@openclaw/<name>` naming
- `openclaw.plugin.json` with plugin metadata
- `index.ts` default-exporting `{ id, name, description, register(api) }`
- `register(api)` uses `api.registerTool()`, `api.registerChannel()`, etc.

## 7) Skill contract
Skills live in `skills/<name>/` and contain:
- `SKILL.md` with YAML frontmatter (name, description, metadata)
- Instructions for using the skill's tooling

## 8) Quality gates (Definition of Done)
A task is "DONE" only when:
- Acceptance criteria are met.
- Code builds (`pnpm typecheck`).
- Tests are added/updated and pass (`pnpm test`).
- Lint passes (`pnpm lint`).
- No secrets in repo.
- Walkthrough is updated and accurate.

## 9) Coding standards
- Strict TypeScript, no `any` types
- ESM modules (`"type": "module"`)
- Use `.js` extension in import paths
- Files: `kebab-case.ts`
- Classes/Interfaces: `PascalCase`
- Functions/Variables: `camelCase`
- Constants: `UPPER_SNAKE_CASE`

## 10) Output discipline
- Prefer creating/updating files, not long chat explanations.
- Keep changes minimal but correct.
- Don't invent requirements. If ambiguous, write assumptions in the walkthrough.

## 11) Commands

| Command | Action |
|---------|--------|
| `next task` | Read `.agent/rules/autonomous-workflow.md`, execute |
| `code review` | Read `.agent/rules/code-review-workflow.md`, execute |
| `fast track: <X>` | Read `.agent/rules/fast-track-workflow.md`, execute |
| `full audit` | Read `.agent/rules/full-audit-workflow.md`, execute |
| `process findings` | Read `.agent/rules/findings-processing-workflow.md`, execute |
| `pr` | Read `.agent/rules/pr-workflow.md`, execute |

## 12) Pull request creation
When the user says **"pr"**:
1. Read `.agent/rules/pr-workflow.md`
2. Verify current branch is not `main`
3. Push to remote if needed
4. Create PR using `gh pr create` with the template from `.github/PULL_REQUEST_TEMPLATE/default.md`
5. Fill ALL sections of the template
6. Report PR URL to user
