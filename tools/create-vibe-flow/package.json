{
  "name": "create-vibe-flow",
  "version": "0.1.0",
  "type": "module",
  "description": "Create a new vibe-flow project with zero configuration",
  "bin": {
    "create-vibe-flow": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/**/*.ts test/**/*.ts --no-cache",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "@vitest/coverage-v8": "4.0.18",
    "eslint": "^10.0.3",
    "typescript": "^5.7.0",
    "vitest": "4.0.18"
  }
}
